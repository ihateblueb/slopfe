import { ScrollView, Text, View } from 'react-native';
import styles from '@/styles/styles';

const Notifications = () => {
    return (
        <ScrollView>
            <Text style={styles.base.text}>Notifications tbd</Text>
        </ScrollView>
    );
};

export default Notifications;
